2. Write function to get n numbers from the user.

#include<stdio.h>
int number(int a);
void main()
{
    int n;

    number(n);
}

int number(int a)
{
    int i,b[10],n;
    printf("\n enter the size of the numbers");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        scanf("%d",&b[i]);
    }
    
    printf("\n the elements are:");
    for(i=0;i<n;i++)
    {
        printf("%d\t",b[i]);
    }
    
}
